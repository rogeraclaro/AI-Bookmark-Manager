---
phase: 06-final-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/shared/types.ts
  - extension/shared/config.ts
  - src/App.tsx
  - src/package.json.bak
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run build exits with zero errors after all four changes"
    - "npm run test shows 28/28 vitest tests green (same count as before)"
    - "The string 'GET_BOOKMARKS' does not appear anywhere in the codebase"
    - "The string 'NO_CATEGORY' does not appear anywhere in the codebase"
    - "The string 'Gemini ha descartat' does not appear anywhere in the codebase"
    - "The file src/package.json.bak does not exist"
  artifacts:
    - path: "extension/shared/types.ts"
      provides: "Message union without GET_BOOKMARKS"
      contains: "type: 'GET_METADATA' | 'SAVE_BOOKMARK' | 'CHECK_DUPLICATE' | 'GET_CATEGORIES' | 'ADD_CATEGORY';"
    - path: "extension/shared/config.ts"
      provides: "ERRORS object without NO_CATEGORY key"
    - path: "src/App.tsx"
      provides: "Rejected-tweets UI string referencing Claude, not Gemini"
  key_links:
    - from: "extension/shared/types.ts line 23"
      to: "TypeScript compiler"
      via: "npm run build (tsc -b)"
      pattern: "no 'GET_BOOKMARKS' after removal"
    - from: "extension/shared/config.ts line 15"
      to: "TypeScript compiler"
      via: "npm run build (tsc -b)"
      pattern: "no 'NO_CATEGORY' after removal"
---

<objective>
Remove four isolated tech debt items from the v1.0 milestone audit: one dead type union member, one dead ERRORS constant, one stale Gemini brand string, and one leftover .bak file.

Purpose: These items are misleading relics of the Gemini-to-Claude migration. They do not affect runtime behaviour but create confusion when reading the code (wrong AI name in UI, lying type union, exported constant with no consumer, backup file with uninstalled dependency).
Output: Clean codebase where the type union, ERRORS object, and UI strings accurately reflect the current Claude-only architecture.
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-final-polish/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove GET_BOOKMARKS from Message union and ERRORS.NO_CATEGORY from config</name>
  <files>extension/shared/types.ts, extension/shared/config.ts</files>
  <action>
    Two surgical line edits — no surrounding code touched.

    **extension/shared/types.ts line 23:**
    Current:
      type: 'GET_METADATA' | 'SAVE_BOOKMARK' | 'CHECK_DUPLICATE' | 'GET_CATEGORIES' | 'ADD_CATEGORY' | 'GET_BOOKMARKS';
    After (delete the trailing ` | 'GET_BOOKMARKS'` segment including the leading pipe and space):
      type: 'GET_METADATA' | 'SAVE_BOOKMARK' | 'CHECK_DUPLICATE' | 'GET_CATEGORIES' | 'ADD_CATEGORY';

    Pitfall: Do not leave a dangling `|` — remove the entire ` | 'GET_BOOKMARKS'` token.
    All other Message interface members (type, data?) remain unchanged.

    **extension/shared/config.ts line 15:**
    Current:
      NO_CATEGORY: "Selecciona almenys una categoria",
    After: delete this line entirely.

    The ERRORS object surrounding lines must not be reordered, reformatted, or otherwise altered. Only line 15 is removed. The comma on the preceding `TITLE_TOO_LONG` line remains as-is (the next line `DUPLICATE:` already follows without needing a comma adjustment since the deleted line was in the middle, not last).

    Do not touch any other key in the ERRORS object. Do not touch config.ts beyond this one line.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/AI Bookmark Manager/ai-bookmarks" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` exits 0 with no TypeScript errors.
    grep for 'GET_BOOKMARKS' returns zero matches in extension/shared/types.ts.
    grep for 'NO_CATEGORY' returns zero matches in extension/shared/config.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace stale "Gemini" UI string in App.tsx and delete src/package.json.bak</name>
  <files>src/App.tsx, src/package.json.bak</files>
  <action>
    **src/App.tsx line 1608:**
    Current:
      Gemini ha descartat {rejectedTweets.length} tweet{rejectedTweets.length !== 1 ? 's' : ''} perquè
    After (single word substitution — only "Gemini" → "Claude"):
      Claude ha descartat {rejectedTweets.length} tweet{rejectedTweets.length !== 1 ? 's' : ''} perquè

    Do not change any surrounding JSX, whitespace, or adjacent lines. This is a one-word replacement inside a JSX text node. The rest of the rejected-tweets modal (lines 1600-1619) must remain identical.

    **src/package.json.bak:**
    Delete the file. It is an untracked backup artifact from Phase 5 containing stale @google/genai reference. No build tool, script, or import references this path. The root package.json governs the build.

    Command: rm "/Users/rogermasellas/AI/AI Bookmark Manager/ai-bookmarks/src/package.json.bak"
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/AI Bookmark Manager/ai-bookmarks" && npm run build 2>&1 | tail -5 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <done>
    `npm run build` exits 0.
    `npm run test` shows 28/28 vitest tests passing (proxy TAP exit-1 is pre-existing and unrelated).
    The string "Gemini ha descartat" does not appear in src/App.tsx.
    The file src/package.json.bak does not exist.
  </done>
</task>

</tasks>

<verification>
Final state checks after both tasks complete:

1. `npm run build` — exits 0, no tsc errors (authoritative: proves no dangling references to removed type/constant)
2. `npm run test` — 28/28 vitest tests green (regression guard: proves no behavioral breakage)
3. `grep -r 'GET_BOOKMARKS' extension/` — zero matches
4. `grep -r 'NO_CATEGORY' extension/` — zero matches
5. `grep 'Gemini ha descartat' src/App.tsx` — zero matches
6. `ls src/package.json.bak` — "No such file or directory"
</verification>

<success_criteria>
- All four named tech debt items are removed
- Build passes (tsc + vite) with zero errors
- 28 vitest tests remain green
- No adjacent code modified beyond the four named locations
</success_criteria>

<output>
After completion, create `.planning/phases/06-final-polish/06-01-SUMMARY.md` following the summary template at @/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</output>
