---
phase: 04-nyquist-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-claude-proxy/01-VALIDATION.md
  - .planning/phases/02-chrome-tabs-feature/02-VALIDATION.md
  - .planning/phases/03-fix-ai03-single-save/03-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "01-VALIDATION.md has nyquist_compliant: true and reflects the real node:test runner, real test file path proxy/test/proxy.test.mjs, and all 8 requirement rows green"
    - "02-VALIDATION.md exists with nyquist_compliant: true and documents 17/17 vitest tests green across 3 test files"
    - "03-VALIDATION.md exists with nyquist_compliant: true and documents 6/6 vitest tests green in single-save.test.ts"
    - "All three VALIDATION.md files use post-execution style: no pending statuses, no Wave 0 gaps section"
  artifacts:
    - path: ".planning/phases/01-claude-proxy/01-VALIDATION.md"
      provides: "Corrected Phase 1 validation document"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/02-chrome-tabs-feature/02-VALIDATION.md"
      provides: "New Phase 2 validation document"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/03-fix-ai03-single-save/03-VALIDATION.md"
      provides: "New Phase 3 validation document"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/01-claude-proxy/01-VALIDATION.md"
      to: "proxy/test/proxy.test.mjs"
      via: "test command reference"
      pattern: "node --test proxy/test/proxy.test.mjs"
    - from: ".planning/phases/02-chrome-tabs-feature/02-VALIDATION.md"
      to: "extension/tests/tabs-*.test.ts"
      via: "test command reference"
      pattern: "cd extension && npm test"
    - from: ".planning/phases/03-fix-ai03-single-save/03-VALIDATION.md"
      to: "extension/tests/single-save.test.ts"
      via: "test command reference"
      pattern: "cd extension && npm test"
---

<objective>
Bring Phases 1, 2, and 3 into Nyquist validation compliance by rewriting Phase 1's VALIDATION.md (which contains entirely wrong content from before execution) and creating new VALIDATION.md files for Phases 2 and 3 (which have none).

Purpose: The planning workflow tracks Nyquist compliance per phase. Phase 1's existing document is invalid — wrong framework (jest instead of node:test), wrong test paths, all statuses pending. Phases 2 and 3 have no VALIDATION.md at all. This plan creates accurate post-execution documentation matching what was actually built and verified.

Output: Three VALIDATION.md files, all with nyquist_compliant: true, using post-execution style (all statuses green, real test commands, real file paths, no Wave 0 gaps).
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-nyquist-validation/04-CONTEXT.md
@.planning/phases/04-nyquist-validation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 1 VALIDATION.md</name>
  <files>.planning/phases/01-claude-proxy/01-VALIDATION.md</files>
  <action>
Overwrite `.planning/phases/01-claude-proxy/01-VALIDATION.md` completely. The current file is wrong in every dimension: wrong framework, wrong commands, wrong file paths, all statuses pending. Replace it with a post-execution document reflecting the actual verified state.

Frontmatter to write (exact values):
```yaml
---
phase: 1
slug: claude-proxy
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-12
---
```

Test Infrastructure table — use node:test (NOT jest, NOT vitest; the proxy uses Node.js built-in test runner):
- Framework: `node:test` (Node.js built-in)
- Config file: none — no config needed for node:test
- Quick run command: `node --test proxy/test/proxy.test.mjs`
- Full suite command: `node --test proxy/test/proxy.test.mjs`
- Estimated runtime: ~5 seconds

Sampling Rate section — update all commands to use `node --test proxy/test/proxy.test.mjs` for both quick and full runs.

Per-Requirement Verification Map (use requirement-level rows, one per requirement — do NOT invent task IDs that may not match plan internals):

| Requirement | Plan(s) | Description | Test Type | Automated Command | Status |
|-------------|---------|-------------|-----------|-------------------|--------|
| PROXY-01 | 01-01, 01-02 | Local server reads Claude CLI session token | unit | `node --test proxy/test/proxy.test.mjs` | ✅ complete |
| PROXY-02 | 01-01, 01-02 | Local HTTP on localhost:3838 accepts AI requests | unit | `node --test proxy/test/proxy.test.mjs` | ✅ complete |
| PROXY-03 | 01-02 | LaunchAgent auto-start at login on both Macs | manual | bash proxy/test/test-install.sh | ✅ complete |
| PROXY-04 | 01-01, 01-02, 01-04 | Web app and extension call local proxy instead of Gemini | integration | `node --test proxy/test/proxy.test.mjs` | ✅ complete |
| AI-01 | 01-03 | claudeService.ts replaces geminiService.ts with same interface | unit | `npx tsc --noEmit` (from src/) | ✅ complete |
| AI-02 | 01-03 | Tweet processing via Claude (categorization, title, description, isAI) | integration | `node --test proxy/test/proxy.test.mjs` | ✅ complete |
| AI-03 | 01-04 | Extension webpage categorization via Claude when saving | integration | manual (extension E2E) | ✅ complete |
| AI-04 | 01-01, 01-03, 01-04 | Error handling and fallback when proxy unreachable | unit | `node --test proxy/test/proxy.test.mjs` | ✅ complete |

Wave 0 Requirements section: Write "None — all test infrastructure existed and passed before this document was created. Phase 1 executed with 8 tests, all passing (pass 8 / fail 0)."

Manual-Only Verifications section — list the three human verification items from the VERIFICATION.md (LaunchAgent auto-start, extension E2E, tweet import pipeline), each marked as ✅ approved (already verified during execution 2026-03-13).

Validation Sign-Off section — all checkboxes checked, Approval: complete (2026-03-13).
  </action>
  <verify>
    <automated>grep "nyquist_compliant: true" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/01-claude-proxy/01-VALIDATION.md && grep "node --test proxy/test/proxy.test.mjs" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/01-claude-proxy/01-VALIDATION.md && grep -v "jest" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/01-claude-proxy/01-VALIDATION.md && grep -v "pending" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/01-claude-proxy/01-VALIDATION.md | grep -c "complete"</automated>
  </verify>
  <done>01-VALIDATION.md has nyquist_compliant: true; references node --test (not jest); all statuses show ✅ complete; test command points to proxy/test/proxy.test.mjs</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 2 VALIDATION.md</name>
  <files>.planning/phases/02-chrome-tabs-feature/02-VALIDATION.md</files>
  <action>
Create `.planning/phases/02-chrome-tabs-feature/02-VALIDATION.md` from scratch. This file does not exist. Use post-execution style throughout — all statuses green from the start.

Frontmatter:
```yaml
---
phase: 2
slug: chrome-tabs-feature
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-13
---
```

Test Infrastructure table:
- Framework: vitest ^4.1.0
- Config file: `extension/vitest.config.ts`
- Quick run command: `cd extension && npm test`
- Full suite command: `cd extension && npm test`
- Estimated runtime: ~10 seconds

Sampling Rate: After every task commit and plan wave, run `cd extension && npm test`. Full suite before verify-work. Max feedback latency: ~10 seconds.

Per-Requirement Verification Map (one row per requirement):

| Requirement | Plan(s) | Description | Test Type | Automated Command | File(s) | Status |
|-------------|---------|-------------|-----------|-------------------|---------|--------|
| TABS-01 | 02-01, 02-02 | Tabs section in popup showing open Chrome tabs | unit+manual | `cd extension && npm test` | `extension/tests/tabs-filter.test.ts` | ✅ complete |
| TABS-02 | 02-01, 02-02 | Filter tabs by Chrome group or ungrouped view | unit | `cd extension && npm test` | `extension/tests/tabs-filter.test.ts` | ✅ complete |
| TABS-03 | 02-01, 02-02 | Select multiple tabs via checkboxes | unit | `cd extension && npm test` | `extension/tests/tabs-selection.test.ts` | ✅ complete |
| TABS-04 | 02-01, 02-03 | Claude categorizes each selected tab; bulk save | unit+manual | `cd extension && npm test` | `extension/tests/tabs-save.test.ts` | ✅ complete |

Test file breakdown (exactly 17 tests total across 3 files — do NOT include single-save.test.ts, that belongs to Phase 3):
- `extension/tests/tabs-filter.test.ts` — 5 tests (filterTabsByGroup, hasGroups)
- `extension/tests/tabs-selection.test.ts` — 8 tests (toggleTabSelection, selectAllVisible, deselectAllVisible, getSelectionCount)
- `extension/tests/tabs-save.test.ts` — 6 tests (buildTabBookmark, getTabSaveSummary) — note: these are labeled 6 tests (2 describes, 6 its) in the VERIFICATION.md; use 6.

Automated test count: 17/17 tests passing (confirmed 2026-03-13).

Wave 0 Requirements section: "None — vitest infrastructure (extension/vitest.config.ts, devDependencies) was created in Plan 02-01 as the first plan task. All test files exist and are green."

Manual-Only Verifications: Full end-to-end flow in Chrome — ✅ approved by user during Plan 03 Task 2 human checkpoint (2026-03-13). Describes the visual flow: tabs view default, group filter bar, selection persistence across filter switches, confirmation dialog, animated per-row spinner during save, summary screen with categories, retry button, Tancar closes popup.

Validation Sign-Off: all checkboxes checked, Approval: complete (2026-03-13).
  </action>
  <verify>
    <automated>grep "nyquist_compliant: true" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/02-chrome-tabs-feature/02-VALIDATION.md && grep "cd extension && npm test" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/02-chrome-tabs-feature/02-VALIDATION.md && grep "17/17" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/02-chrome-tabs-feature/02-VALIDATION.md</automated>
  </verify>
  <done>02-VALIDATION.md exists with nyquist_compliant: true; documents 17/17 vitest tests; references only tabs-filter.test.ts, tabs-selection.test.ts, tabs-save.test.ts (not single-save.test.ts); all statuses green</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 3 VALIDATION.md</name>
  <files>.planning/phases/03-fix-ai03-single-save/03-VALIDATION.md</files>
  <action>
Create `.planning/phases/03-fix-ai03-single-save/03-VALIDATION.md` from scratch. This file does not exist. Use post-execution style throughout.

Frontmatter:
```yaml
---
phase: 3
slug: fix-ai03-single-save
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-14
---
```

Test Infrastructure table:
- Framework: vitest ^4.1.0
- Config file: `extension/vitest.config.ts`
- Quick run command: `cd extension && npm test`
- Full suite command: `cd extension && npm test`
- Estimated runtime: ~10 seconds
- Note: Full suite runs 23 tests (Phase 2's 17 + Phase 3's 6); Phase 3's own contribution is 6/6 in single-save.test.ts.

Per-Requirement Verification Map:

| Requirement | Plan | Description | Test Type | Automated Command | File | Status |
|-------------|------|-------------|-----------|-------------------|------|--------|
| AI-03 | 03-01 | Single-page save path calls Claude proxy for categorization, title, description | unit+manual | `cd extension && npm test` | `extension/tests/single-save.test.ts` | ✅ complete |

Test file: `extension/tests/single-save.test.ts` — 6 tests covering all three fallback tiers of resolveSaveCategories:
- Claude returns valid categories → uses them
- Claude returns no valid categories + user selected → uses user selection
- Claude returns no valid categories + user selected nothing → uses 'Altres'
- Proxy unreachable (empty array) + user selected → uses user selection
- Proxy unreachable (empty array) + nothing selected → uses 'Altres'
- (integration context) handleSave wiring (wired to callClaudeProxy + resolveSaveCategories)

Automated test count: 6/6 tests passing. Full suite: 23/23 passing (no regressions).

Wave 0 Requirements section: "None — vitest infrastructure was inherited from Phase 2 (extension/vitest.config.ts, devDependencies). single-save.test.ts was created during Plan 03-01 execution using TDD (red→green cycle)."

Manual-Only Verifications: Three scenarios approved by user during Plan 03-01 Task 3 human checkpoint (2026-03-14):
1. Claude proxy E2E single-page save — leave category picker empty, click Save, bookmark gets Claude-generated categories — ✅ approved
2. Proxy-down fallback with user-selected categories — manually selected category preserved, no 'Altres' — ✅ approved
3. Proxy-down + no selection — bookmark saved with 'Altres' — ✅ approved

Validation Sign-Off: all checkboxes checked, Approval: complete (2026-03-14).
  </action>
  <verify>
    <automated>grep "nyquist_compliant: true" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/03-fix-ai03-single-save/03-VALIDATION.md && grep "single-save.test.ts" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/03-fix-ai03-single-save/03-VALIDATION.md && grep "6/6" /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks/.planning/phases/03-fix-ai03-single-save/03-VALIDATION.md</automated>
  </verify>
  <done>03-VALIDATION.md exists with nyquist_compliant: true; documents 6/6 vitest tests in single-save.test.ts; documents 23/23 full suite count; all three human verification items marked approved; all statuses green</done>
</task>

</tasks>

<verification>
After all three tasks complete, confirm:

```bash
# All three files exist
ls .planning/phases/01-claude-proxy/01-VALIDATION.md \
   .planning/phases/02-chrome-tabs-feature/02-VALIDATION.md \
   .planning/phases/03-fix-ai03-single-save/03-VALIDATION.md

# All three have nyquist_compliant: true
grep "nyquist_compliant: true" \
  .planning/phases/01-claude-proxy/01-VALIDATION.md \
  .planning/phases/02-chrome-tabs-feature/02-VALIDATION.md \
  .planning/phases/03-fix-ai03-single-save/03-VALIDATION.md

# Phase 1 no longer references jest
grep -v "jest" .planning/phases/01-claude-proxy/01-VALIDATION.md
```
</verification>

<success_criteria>
- All three VALIDATION.md files exist
- All three have `nyquist_compliant: true` in frontmatter and `status: complete`
- 01-VALIDATION.md references `node --test proxy/test/proxy.test.mjs` (not jest, not npm test)
- 01-VALIDATION.md has 8 requirement rows (PROXY-01 through AI-04), all ✅ complete
- 02-VALIDATION.md documents 17/17 vitest tests across tabs-filter, tabs-selection, tabs-save test files
- 03-VALIDATION.md documents 6/6 vitest tests in single-save.test.ts; full suite 23/23
- No file uses pending statuses or Wave 0 gaps sections listing missing files
</success_criteria>

<output>
After completion, create `.planning/phases/04-nyquist-validation/04-01-SUMMARY.md` following the standard summary template.
</output>
