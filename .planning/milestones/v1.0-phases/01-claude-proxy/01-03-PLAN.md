---
phase: 01-claude-proxy
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/services/claudeService.ts
  - src/App.tsx
  - .env
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-04

must_haves:
  truths:
    - "claudeService.ts exports `processBookmarksWithClaude` with the same signature as `processBookmarksWithGemini`"
    - "`tsc --noEmit` passes with no type errors after the changes"
    - "App.tsx imports from claudeService, not geminiService"
    - "TrialCountdown component is removed from App.tsx render tree and import"
    - ".env contains VITE_CLAUDE_PROXY_URL=http://localhost:3838"
    - "When proxy returns 200 with a valid body, processBookmarksWithClaude returns ProcessedTweetResult[]"
    - "When proxy is unreachable (connection refused / timeout), fallback bookmark is created from raw tweet text — no crash"
  artifacts:
    - path: "src/services/claudeService.ts"
      provides: "Drop-in replacement for geminiService.ts using proxy HTTP calls"
      exports: ["processBookmarksWithClaude"]
    - path: "src/App.tsx"
      provides: "Wired to claudeService, TrialCountdown removed"
  key_links:
    - from: "src/services/claudeService.ts"
      to: "http://localhost:3838/process-tweet"
      via: "fetch with AbortSignal forwarded from App.tsx"
      pattern: "fetch.*process-tweet"
    - from: "src/App.tsx"
      to: "processBookmarksWithClaude"
      via: "import from ./services/claudeService"
      pattern: "import.*claudeService"
---

<objective>
Create `claudeService.ts` as a drop-in replacement for `geminiService.ts`, wired to the local proxy. Update App.tsx to use it and remove Gemini-specific UI (TrialCountdown).

Purpose: The web app's tweet import pipeline must run through Claude instead of Gemini, with identical retry/fallback behavior.
Output: `src/services/claudeService.ts`, updated `src/App.tsx`, updated `.env`.
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-claude-proxy/01-RESEARCH.md
@src/services/geminiService.ts
@src/App.tsx
@src/types.ts

<interfaces>
<!-- From geminiService.ts — interface to replicate exactly -->

```typescript
// Public signature (must match in claudeService.ts — App.tsx calls this)
export const processBookmarksWithClaude = async (
  rawTweets: TweetRaw[],
  currentCategories: string[],
  onProgress: (current: number, total: number) => void,
  onLog: (message: string, type: 'info' | 'success' | 'warning' | 'error') => void,
  signal?: AbortSignal
): Promise<ProcessedTweetResult[]>

// Types from src/types.ts
interface TweetRaw {
  full_text?: string;
  text?: string;
  id_str?: string;
  id?: string;
  entities?: { urls?: Array<{ expanded_url: string }> };
  [key: string]: any;
}

interface ProcessedTweetResult {
  isAI: boolean;
  title: string;
  categories: string[];
  externalLinks: string[];
  originalId: string;
}
```

<!-- From proxy/server.js (Plan 02) — endpoint contract -->

POST http://localhost:3838/process-tweet
Body: { tweet: { id: string, text: string, urls: string[] }, categories: string[] }
Response: { originalId, isAI, title, categories, externalLinks } | fallback shape

<!-- From App.tsx — integration points -->

Line 20: `import { processBookmarksWithGemini } from './services/geminiService'`  → replace
Line 23: `import { TrialCountdown } from './components/TrialCountdown'`           → remove
Line 410: `const processed = await processBookmarksWithGemini(`                   → replace call
Line 1839: `<TrialCountdown />`                                                   → remove JSX

VITE_CLAUDE_PROXY_URL is read via: `import.meta.env.VITE_CLAUDE_PROXY_URL`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create claudeService.ts</name>
  <files>src/services/claudeService.ts</files>
  <behavior>
    - processBookmarksWithClaude has identical TypeScript signature to processBookmarksWithGemini
    - For each tweet: fetches POST /process-tweet on the proxy with {tweet: {id, text, urls}, categories}
    - BATCH_SIZE=1, one tweet at a time, same as geminiService.ts
    - 90-second timeout per request using AbortSignal.timeout(90000) on fetch
    - Retry logic: max 3 retries per tweet with 2-second delay between retries (no rate limit concern since proxy is local)
    - If signal.aborted, throws DOMException("Aborted", "AbortError") — same as geminiService.ts
    - On all retries exhausted OR fetch throws (ECONNREFUSED/network error), creates fallback bookmark: { originalId, isAI: false, title: first 80 chars of raw text, categories: ['Altres'], externalLinks: non-twitter external URLs }
    - Calls onProgress(i, total) before each tweet
    - Calls onLog("Analitzant tweet X de Y...", 'info') before each tweet
    - Calls onLog("Tweet processat correctament", 'success') on success
    - Calls onLog with warning on retry attempts
    - Calls onLog with warning when creating fallback bookmark
    - Does NOT export getTrialInfo — Gemini trial logic removed entirely
    - sanitizeText helper (port from geminiService.ts): strips hashtags, mentions, newlines, control chars, truncates to 700 chars
  </behavior>
  <action>
Create `src/services/claudeService.ts`. Do NOT copy the Gemini trial configuration, rate limiting constants, or JSON parsing hacks — Claude's structured output via the proxy eliminates those concerns.

Structure:
1. Constants: BATCH_SIZE=1, MAX_RETRIES=3, DELAY_MS=2000, TIMEOUT_MS=90000
2. `sanitizeText(text: string): string` — port directly from geminiService.ts lines 61-71
3. `processBookmarksWithClaude(...)` — main export

The `processBookmarksWithClaude` function:
- Reads proxy URL from `import.meta.env.VITE_CLAUDE_PROXY_URL` with fallback `'http://localhost:3838'`
- For each tweet in `validTweets` (filtered for those with text):
  - Retry loop (up to MAX_RETRIES):
    - `fetch(`${proxyUrl}/process-tweet`, { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({tweet: {id, text: sanitized, urls}, categories}), signal: AbortSignal.timeout(TIMEOUT_MS) })`
    - Forward the outer `signal` abort by checking `signal?.aborted` at the top of each iteration
    - On success (response.ok): push result to results array, break retry loop
    - On HTTP error or fetch throw: increment attempts, log warning, delay 2s
  - After all retries exhausted: create fallback ProcessedTweetResult and push it (mark isAI: false, categories: ['Altres'])
- Return results after all tweets processed

Use `import type { TweetRaw, ProcessedTweetResult } from '../types'`

After writing, verify TypeScript types compile:
```bash
cd /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/AI Bookmark Manager/ai-bookmarks" && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>`tsc --noEmit` exits 0 (no type errors). File `src/services/claudeService.ts` exists and exports `processBookmarksWithClaude` with the correct TypeScript signature.</done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx to claudeService + add env var</name>
  <files>src/App.tsx, .env</files>
  <action>
Make the following surgical changes to `src/App.tsx`:

1. **Replace import (line 20):**
   - Remove: `import { processBookmarksWithGemini } from './services/geminiService'`
   - Add: `import { processBookmarksWithClaude } from './services/claudeService'`

2. **Remove TrialCountdown import (line 23):**
   - Remove: `import { TrialCountdown } from './components/TrialCountdown'`

3. **Replace function call (line ~410):**
   - Replace: `const processed = await processBookmarksWithGemini(`
   - With: `const processed = await processBookmarksWithClaude(`
   - All arguments remain identical — no other changes needed on these lines.

4. **Remove TrialCountdown JSX (line ~1839):**
   - Remove: `<TrialCountdown />`
   - Do NOT remove any surrounding JSX structure — only the component tag itself.

5. **Do NOT touch any other code in App.tsx.** Do not reformat, do not clean up, do not touch comments.

Then add to `.env`:
```
VITE_CLAUDE_PROXY_URL=http://localhost:3838
```

Keep the existing `.env` contents (VITE_API_KEY, VITE_STORAGE_API_URL, VITE_STORAGE_SECRET) — append the new line.

After changes, verify TypeScript compiles:
```bash
cd /Users/rogermasellas/AI/AI\ Bookmark\ Manager/ai-bookmarks && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/AI Bookmark Manager/ai-bookmarks" && npx tsc --noEmit 2>&1 && grep "claudeService" src/App.tsx && grep "VITE_CLAUDE_PROXY_URL" .env</automated>
  </verify>
  <done>`tsc --noEmit` exits 0. `src/App.tsx` imports from `claudeService` and no longer references `geminiService` or `TrialCountdown`. `.env` contains `VITE_CLAUDE_PROXY_URL=http://localhost:3838`.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 with no type errors
- `grep "geminiService\|TrialCountdown" src/App.tsx` returns nothing (imports and JSX removed)
- `grep "processBookmarksWithClaude" src/App.tsx` returns the function call
- `grep "VITE_CLAUDE_PROXY_URL" .env` returns the env var line
- `grep "processBookmarksWithClaude" src/services/claudeService.ts` confirms export exists
</verification>

<success_criteria>
Web app is wired to Claude proxy. Tweet import pipeline uses `processBookmarksWithClaude`. Gemini trial UI removed. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-claude-proxy/01-03-SUMMARY.md` following the summary template.
</output>
